# Client Debug
---

This was made for sending commands to the ESP32 server, change the IP to the IP of your ESP32 server and try to send a command (See the procotol folder for commands). The server Ip is printed when you run the sketch in the firmware folder.
